# API Base URL for frontend requests
# When using Next.js rewrite proxy to backend: /api/v1
# When hosted separately in production without proxy: https://your-backend-domain.com/api/v1
NEXT_PUBLIC_API_URL="/api/v1"
